exp_name: "'@{dataset.name}'_'@{network.name}'_'@{pipeline.name}'_'@{evaluator.name}'_'@{postprocessor.name}'"
output_dir: ./results/
save_output: True
force_merge: False # disabled if 'save_output' is False

num_gpus: 1
num_workers: 0
num_machines: 1
machine_rank: 0

network:
  pretrained: True
  checkpoint: ./results/cifar10_resnet18_32x32_base_e200_lr0.1/best.ckpt

pipeline:
  name: test_ood

evaluator:
  name: ood

recorder:
  save_scores: True
  save_csv: True
