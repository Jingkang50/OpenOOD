exp_name: "'@{dataset.name}'_'@{network.name}'_'@{pipeline.name}'"
output_dir: ./results/
save_output: False
force_merge: True # disabled if 'save_output' is False

num_gpus: 1
num_workers: 0

network:
  pretrained: True
  checkpoint: ./results/bottle_cutpaste_cutpaste_e100_lr0.1/best_epoch80_loss0.022499999999999964.ckpt
pipeline:
  name: test_ad

evaluator:
  name: cutpaste
